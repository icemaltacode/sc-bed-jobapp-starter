<main class="w-80 m-auto">
    <form id="loginForm" method="POST" action="/action/login">
        <h1 class="h3 mb-3 fw-normal">Please sign in.</h1>
        <div class="form-floating">
            <input type="email" class="form-control" id="email" name="email" placeholder="name@example.com"
                required value="keith@icemalta.com">
            <label for="email">Email address</label>
        </div>
        <div class="form-floating">
            <input type="password" class="form-control" id="password" name="password" placeholder="Password" required value="MyPassword123">
            <label for="password">Password</label>
        </div>
        <input type="hidden" name="redirect" value="{{ redirect }}">
        <button class="btn btn-primary w-100 py-2" type="submit">Sign in</button>
        <a href='/register'>No Account? Create One!</a>
    </form>
</main>
